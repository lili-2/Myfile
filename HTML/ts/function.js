//函数类型表达式
// function greeter(fn: (a: string) => void) {
//     fn("Hello, World");
// }
// //限制Type为{ length: number }
// function longest<Type extends { length: number }>(a: Type, b: Type) {
//     if (a.length >= b.length) {
//       return a;
//     } else {
//       return b;
//     }
//   }
function jspang() {
    var web = "Hello World";
    console.log(web);
}
jspang();
